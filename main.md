Отличный план! Давай пошагово распишем физику этого процесса, чтобы ты чётко понимал, что именно запрашивать у Cursor и в какие папки по методологии FSD раскладывать результат.Пока интернета нет, мы временно сымитируем работу сервера прямо внутри кода.Вот детальный план-инструкция, по которой ты сейчас поведёшь Cursor.Шаг 1. Локальные данные (Mock JSON)Ты уже создал файл questions.mock.json в папке entities/question/api/ (вижу на скриншоте).Что проверить: убедись, что Cursor наполнил его массивом объектов с полями id, title, description, difficulty ('easy' | 'medium' | 'hard') и category.Шаг 2. Создаем слой Фичей (features/FilterQuestions)Фильтрация — это действие пользователя, поэтому создаём для неё отдельную независимую папку.Куда положить: src/features/FilterQuestions/Попроси Cursor создать внутри этой папки следующую структуру:ui/FilterQuestions.tsx — сам компонент панели фильтров (кнопки, инпуты, селекты).model/useFilters.ts — кастомный хук для управления состоянием фильтров.index.ts — главный фасад фичи (Public API).Что запросить у Cursor для хука useFilters.ts:Локальный useState для хранения объекта фильтров:typescript{ search: string; difficulty: 'easy' | 'medium' | 'hard' | ''; category: string }
Используйте код с осторожностью.Функции-сеттеры, которые этот стейт меняют (например, setSearch, setDifficulty, resetFilters).Что запросить у Cursor для UI-компонента FilterQuestions.tsx:Компонент должен принимать через пропсы текущие значения фильтров и функции их изменения из хука useFilters.Внутри него должны быть: текстовый инпут для поиска, выпадающий список (или группа кнопок) для выбора сложности и кнопка «Сбросить».Что должно быть в фасаде features/FilterQuestions/index.ts:typescriptexport { FilterQuestions } from './ui/FilterQuestions';
export { useFilters } from './model/useFilters';
Используйте код с осторожностью.Шаг 3. Собираем всё в виджете QuestionsBrowserТеперь мы идём в наш главный управляющий блок экрана — src/widgets/QuestionsBrowser/ui/QuestionsBrowser.tsx.Попроси Cursor переписать его так, чтобы он временно работал без RTK Query, чисто на локальном JSON.Логика внутри виджета:Импорт данных: Импортируем массив напрямую:typescriptimport mockData from '@/entities/question/api/questions.mock.json';
Используйте код с осторожностью.Вызов хука фильтров:typescriptconst { filters, setSearch, setDifficulty, resetFilters } = useFilters();
Используйте код с осторожностью.Рендер фичи: Вставляем панель фильтров в шапку виджета:tsx<FilterQuestions filters={filters} onSearchChange={setSearch} ... />
Используйте код с осторожностью.Локальная фильтрация (Физика процесса): Перед тем как отдать данные в список, мы прямо в теле виджета фильтруем наш JSON-массив с помощью стандартных методов JavaScript:typescriptconst filteredQuestions = mockData.filter(question => {
  const matchesSearch = question.title.toLowerCase().includes(filters.search.toLowerCase());
  const matchesDifficulty = filters.difficulty ? question.difficulty === filters.difficulty : true;
  return matchesSearch && matchesDifficulty;
});
Используйте код с осторожностью.Рендер списка: Передаем уже отфильтрованный массив:tsx<QuestionList questions={filteredQuestions} />
Используйте код с осторожностью.В чём крутость такого подхода?Когда интернет вернётся, нам останется сделать всего одно движение: мы перенесём локальный стейт из хука useFilters в URL-строку браузера, а отфильтрованный массив filteredQuestions заменим на реальный data из RTK Query, просто передав параметры фильтров в useFetchQuestionsQuery(filters). Вся вёрстка и структура папок при этом останутся нетронутыми!Ставь задачу Курсору по этим шагам.Если на каком-то этапе (например, при написании пропсов для фичи или методов фильтрации массива) возникнут вопросы по коду — пиши, скидывай код, разберём каждую строчку! На каком ты сейчас шаге?

Готово. Скачаны два файла:

Файл	Содержимое
src/entities/specialization/api/specializations.mock.json
28 специализаций
src/entities/skill/api/skills.mock.json
12 навыков для spec id=11 (React Frontend Developer)
Вопросы уже лежат в src/entities/question/api/questions.mock.json.

Подключение API и entities — на твоей стороне.

Отличный статус-апдейт! Поздравляю с закрытием Куска 5 — архитектурный детокс пройден, страница теперь легкая, а виджет взял на себя управление. Схема полностью соответствует коммерческому референсу.Раз уж мы переходим к Куску 6 (Фильтры в URL), давай двигаться строго по твоему выверенному плану. Первым пунктом у нас идет подготовка данных: создание сущностей specialization и skill по образу и подобию question.Давай разберем физику этого шага. На бэкенде вопросы привязаны к определенным навыкам (например, redux) и специализациям (frontend). Чтобы пользователь мог выбирать их в выпадающих списках, нам нужно сначала эти списки скачать.Твое микро-задание (Шаг 6.1):Создаем сущности specialization и skill. Действуй по аналогии с тем, как мы делали для вопросов:Создай папки в src/entities/:src/entities/specialization/src/entities/skill/Опиши типы данных в model/types.ts для каждой сущности (например, { id: string; name: string; slug: string; } — уточни по своим mock-файлам).Создай API-сервисы specializationApi.ts и skillApi.ts в папках api/.Важно: Не пиши новые базовые инстансы! Используй метод .injectEndpoints нашего центрального baseApi, который мы создали на самом первом шаге. Они должны просто внедрить свои эндпоинты (например, fetchSpecializations и fetchSkills) в общий стор.Открой Public API через файлы index.ts в корне каждой сущности (экспортируй типы и сгенерированные RTK Query хуки).Пока ты без интернета, Cursor может настроить эти эндпоинты так, чтобы они временно перехватывали запросы и отдавали данные из твоих specializations.mock.json и skills.mock.json.Начни с этого фундамента. Как только создашь структуру и настроишь injectEndpoints для новых сущностей, покажи код одного из API-файлов, и мы разберем, правильно ли они подключились к baseApi!Уточнить перед стартом: как у тебя в mock-файлах выглядят объекты специализаций и навыков? Какие там поля?